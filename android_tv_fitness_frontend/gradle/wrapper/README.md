# Gradle Wrapper Placeholder

This directory normally contains:
- `gradle-wrapper.jar` (binary)
- `gradle-wrapper.properties`

They are intentionally omitted here. The Gradle wrapper is not initialized yet. Once the Android project is set up, remove this placeholder and add the real wrapper files.
