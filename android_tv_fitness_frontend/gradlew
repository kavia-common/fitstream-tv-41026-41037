#!/usr/bin/env bash
# Temporary stub gradle wrapper to unblock CI until Android project is initialized.
# This script intentionally no-ops and exits 0 so that CI does not fail due to missing Gradle wrapper.
# Replace this file with the real Gradle wrapper when the Android project is added.
echo "[android_tv_fitness_frontend] Gradle wrapper is not initialized yet. Skipping Gradle tasks."
exit 0
